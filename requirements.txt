# PLMLoF requirements
# Install optional extras separately:
#   pip install ".[tracking]"    — wandb experiment tracking
#   pip install ".[attribution]" — captum gradient attribution
#   pip install ".[dev]"         — pytest for running tests
torch>=2.0,<3.0
transformers>=4.36,<4.55
peft>=0.9,<1.0
datasets>=2.14
biopython>=1.81
pysam>=0.22
PyYAML>=6.0
scikit-learn>=1.3
scipy>=1.11
accelerate>=0.24
pandas>=2.0
tqdm>=4.65
